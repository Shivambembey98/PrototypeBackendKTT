
security:
  jwt:
    token:
      secret-key: ${JWT_SECRET}

server:
  port: 8080
  error:
    include-stacktrace: never
spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}
    username: ${DB_USER}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
  mail:
    host: email-smtp.ap-south-1.amazonaws.com
    username: AKIAUGO4K4LZYP7NC6U7
    password: BG/Mkqxafk53g8P3RTD68Aust/9T8Gt3cO7h9YETcIZO
    protocol: smtp
    port: 587
    properties:
      mail:
        debug: true
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
from:
  email:
    address: ankitsar415@gmail.com
    personal: KTT



